# Overnight report — 2026-08-15 → 16

Installed build: `baf8fbf` on `draft-assistant-overlay-upgrades` (single running instance).
Full record of the collaboration (tasks, reviews, decisions): `/tmp/MTG-DraftFM-Overlay-20260816.md`.

## What landed (all reviewed + independently verified; 246 unit tests, strict e2e green)
- **Scryfall-only set bundles** for all 32 curated sets (26 MB): `assets.npz` + name-keyed `cards.json`
  (printed colours + identity, cost, type, rarity), dated snapshot 2026-08-15 shown in the HUD; frozen
  featurizer manifest ships with the model; no 17Lands anywhere; HOB excluded (Scryfall has no Arena
  ids yet; documented `--arena-ids` escape hatch, no data shipped).
- **Model**: pool-conditioned "for your pool" grade alongside the raw set grade; both paths now
  Python-parity checked (bit-identical logits on the pool-conditioned whole-set curve).
- **Overlay**: unified right rail (HUD + pool, one panel), #1–#5, meta line, pool sorted best→worst with
  pick numbers, ×N grouping, Lands divider, Pool rating; rail bounded above Arena's Sideboard bar with
  internal scroll; rail yields to Arena on dwell (whole panel); Room/split preview prediction; hover
  dwell 350 ms / leave grace 120 ms; right-column flip / bottom-row clamp; overlay reappearance fix;
  idle glyph only outside drafts; complete-phase summary keeps the pool visible.
- **Correctness**: Arena's real display-order keys (gold pairs table, lands in the land tier, title order,
  top-anchored shrinking grid) verified on real packs; duplicate P1P1 history entry on human drafts fixed;
  calibration nearest-bucket uses actual aspect; 13/15-card grids contained.
- **Permissions**: none required (helper-only geometry, no AppleScript; capture opt-in "Precise layering").
- **Perf**: idle timers only while badges are live; capture off unless opted in.
- **Release/docs**: unsigned DMG builds (`npm run package`, ~132 MB), `docs/RELEASE.md`, `docs/TESTING.md`,
  README refreshed; dev tooling: `scripts/dev/pick-next-card.sh` (verified single double-click, land and
  1-card guards), `scripts/dev/screenshot-arena.sh`, `MTGA_STATE_FILE` mirror.

## Live testing (Arena, DSK Quick Draft, pack 2)
- Verified at P2P6: badges on the correct cards, rail content, provenance line, yield behaviour.
- The screen locked at ~22:10; the final live check of the bounded rail (post-`c49814f`) is pending unlock.
- No picks made after P2P6 (per instruction to leave pack 2 to Brian).

## Open / for Brian
- HOB bundle: waits on Scryfall arena_ids (or use `--arena-ids` overlay).
- Optional: sign/notarize for distribution.
